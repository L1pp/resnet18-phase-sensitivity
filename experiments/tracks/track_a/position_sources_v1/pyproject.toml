[build-system]
requires = []
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "position-sources-v1"
version = "0.1.0"
description = "Auditable local Track A position-source experiments"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
